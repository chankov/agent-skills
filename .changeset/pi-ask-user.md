---
"@chankov/agent-skills": minor
---

Publish agent-skills as a first-class pi package and bundle pi-ask-user so pi installs expose the ask_user tool and ask-user skill. Include pi-ask-user's TypeBox runtime peer so the bundled extension can resolve its imports.
