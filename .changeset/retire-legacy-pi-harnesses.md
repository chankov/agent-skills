---
"@chankov/agent-skills": patch
---

Retire legacy standalone pi harnesses that are now consolidated into `agent-hub`, remove their just recipes and `agent-chain` YAML config, and update pi docs/guided setup to list the supported harness set.
