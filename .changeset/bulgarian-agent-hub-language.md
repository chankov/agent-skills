---
"@chankov/agent-skills": patch
---

Teach guided setup and docs about the `agent-hub` user-facing language override. The setup flow now offers the legacy `## agent-team` / `language: <value>` section for pi `agent-hub` targets, and docs/templates show how to configure it while preserving English as the default when the section is omitted.
