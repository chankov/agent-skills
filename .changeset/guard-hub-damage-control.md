---
"@chankov/agent-skills": patch
---

Load the hard-stop `damage-control` harness before `agent-hub` in the default `just hub` and `just hub-solo` recipes, retire the `damage-control-continue` harness and recipe, and update the pi harness docs/setup guidance to describe the guarded hub launch and remaining safety scope.
